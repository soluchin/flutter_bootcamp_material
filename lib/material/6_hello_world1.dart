import 'package:flutter/material.dart';

class HelloWorld1 extends StatelessWidget {
  const HelloWorld1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hello, world!'),
    );
  }
}
